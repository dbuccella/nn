<Window x:Class="nnViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
        xmlns:local="clr-namespace:nnViewer"
        mc:Ignorable="d"
        Title="Plot" Height="501.162" Width="685.465">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <Grid Height="376" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="401*"/>
            <RowDefinition Height="22*"/>
        </Grid.RowDefinitions>
        <oxy:Plot x:Name="MyPlotView" Title="{Binding Title}" Margin="0,10,0,30">
            <oxy:Plot.Series>
                <oxy:LineSeries x:Name="LineSeries1" ItemsSource="{Binding Points}"/>
            </oxy:Plot.Series>
        </oxy:Plot>
        <Button x:Name="button" Content="Run" HorizontalAlignment="Left" Height="33" Margin="254,58,0,-71" Grid.Row="1" VerticalAlignment="Top" Width="149" Click="button_Click" FontSize="14"/>
    </Grid>
</Window>
